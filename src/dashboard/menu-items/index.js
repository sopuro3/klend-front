import dashboard from "./dashboard";
import utilities from "./utilities";
import other from "./other";
import needs from "./needs";

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
    items: [dashboard, needs, utilities, other],
};

export default menuItems;
